# Reference configurations for the Liftr Backstage integration.

See ../README.md and app-config.liftr.example.yaml. The Keycloak compose
profile provides a deterministic local STS for full-auth development and
L3-style testing (enable Standard Token Exchange on the bff client first).
