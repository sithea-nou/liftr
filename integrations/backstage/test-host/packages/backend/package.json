{
  "name": "@liftr/testhost-backend",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "backstage-cli package build",
    "config:check": "backstage-cli config:check --config ../../app-config.yaml",
    "start": "backstage-cli package start --config ../../app-config.yaml"
  },
  "dependencies": {
    "@backstage/backend-defaults": "^0.18.0",
    "@backstage/backend-plugin-api": "^1.10.0",
    "@backstage/plugin-auth-backend": "^0.30.0",
    "@backstage/plugin-auth-backend-module-guest-provider": "^0.2.22",
    "@liftr/plugin-liftr-backend": "workspace:^",
    "better-sqlite3": "^13.0.3"
  },
  "devDependencies": {
    "@backstage/cli": "^0.32.0"
  },
  "backstage": {
    "role": "backend"
  }
}
