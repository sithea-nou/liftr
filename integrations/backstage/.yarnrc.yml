# Liftr Backstage integration workspace.
# Contemporary Backstage repositories use Yarn Berry via corepack; the
# node-modules linker keeps output consumable by host apps resolving through
# node_modules.
nodeLinker: node-modules
